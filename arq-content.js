(function(){'use strict';if(window.__ARQ_CONTENT_620__)return;window.__ARQ_CONTENT_620__=true;const A=()=>window.ARQSELECT6,$=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const api=(a,d={})=>A()?.api?A().api(a,d):Promise.resolve({sucesso:false});function card(x){return '<article class="arq6-card arq-content-card">'+(x.imagem?'<img class="arq-media-wide" src="'+esc(x.imagem)+'" alt="" loading="lazy">':'')+'<div class="arq6-kicker">'+esc(x.categoria||x.tipo||'ARQSELECT')+'</div><h3>'+esc(x.titulo||x.nome||'Conteúdo')+'</h3><p class="arq6-muted">'+esc(x.resumo||x.descricao||'')+'</p>'+((x.data||x.local)?'<small>'+esc([x.data,x.local].filter(Boolean).join(' · '))+'</small>':'')+(x.url?'<a class="arq6-btn" href="'+esc(x.url)+'">Abrir</a>':'')+'</article>'}async function load(){const host=$('#contentResults'),tipo=host.dataset.contentType,r=await api('public_conteudos',{tipo,limite:60}),rows=r.itens||r.conteudos||[];host.innerHTML=rows.length?rows.map(card).join(''):'<div class="arq6-empty"><b>Nenhum conteúdo publicado ainda.</b><p>A ARQSELECT não inventa cases, depoimentos ou eventos. Esta área será preenchida com conteúdo real e autorizado.</p></div>'}document.addEventListener('DOMContentLoaded',load,{once:true});})();