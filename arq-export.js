(function(){'use strict';if(window.__ARQ_EXPORT_630__)return;window.__ARQ_EXPORT_630__=true;const A=()=>window.ARQSELECT6,$=s=>document.querySelector(s),token=()=>localStorage.getItem('ARQSELECT_PORTAL_TOKEN')||'';const api=(a,d={})=>A()?.api?A().api(a,{...d,token:token()}):Promise.resolve({sucesso:false});function csv(rows){if(!rows.length)return'';const keys=Object.keys(rows[0]);const q=v=>'"'+String(v??'').replace(/"/g,'""')+'"';return keys.map(q).join(';')+'\n'+rows.map(r=>keys.map(k=>q(r[k])).join(';')).join('\n')}document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-export]').forEach(b=>b.onclick=async()=>{const tipo=b.dataset.export;$('#exportStatus').textContent='Preparando '+tipo.toLowerCase()+'…';const r=await api('portal_exportacao',{tipo,projetoId:new URLSearchParams(location.search).get('projectId')||''});if(!r.sucesso){$('#exportStatus').textContent=r.mensagem||'Não foi possível exportar.';return}const rows=r.registros||[],blob=new Blob([csv(rows)],{type:'text/csv;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='arqselect-'+tipo.toLowerCase()+'-'+new Date().toISOString().slice(0,10)+'.csv';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);$('#exportStatus').textContent=rows.length+' registro(s) exportados.'})},{once:true});})();