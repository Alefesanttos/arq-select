(function(){'use strict';if(window.__ARQ_MAP_630__)return;window.__ARQ_MAP_630__=true;const A=()=>window.ARQSELECT6,$=s=>document.querySelector(s),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){const city=$('#mapCity').value.trim(),state=$('#mapState').value.trim(),type=$('#mapType').value,r=await A().api('public_mapa_rede',{cidade:city,estado:state,tipo:type}),rows=r.itens||r.resultados||[];$('#mapSummary').innerHTML='<p><b>'+rows.length+'</b> resultado(s) com localização profissional publicada.</p>';$('#mapResults').innerHTML=rows.length?rows.map(x=>'<article class="arq6-card"><div class="arq6-kicker">'+esc(x.tipo||'REDE')+'</div><h3>'+esc(x.nome||'Profissional')+'</h3><p>'+esc([x.cidade,x.estado].filter(Boolean).join(' · '))+'</p><p class="arq6-muted">'+esc(x.areaAtendimento||x.categoria||'')+'</p><a class="arq6-btn" href="'+esc(x.url||'#')+'">Ver perfil</a></article>').join(''):'<div class="arq6-empty">Nenhum perfil público encontrado nesta região.</div>'}document.addEventListener('DOMContentLoaded',()=>{$('#mapSearch').onclick=load;load()},{once:true});})();