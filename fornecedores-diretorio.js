(function(){'use strict';const A=window.ARQ,M=window.ARQSELECT_MEDIA,section=document.createElement('section');section.className='arq-market arq-section';section.setAttribute('aria-labelledby','partnerDirectoryTitle');section.innerHTML='<div class="arq-section__head"><div><div class="arq-kicker">REDE ARQSELECT</div><h2 id="partnerDirectoryTitle">Fornecedores parceiros</h2><p>Conheça os perfis institucionais aprovados na plataforma.</p></div><a class="arq-btn" href="explorar.html">Explorar produtos</a></div><div id="partnerDirectory" class="arq-supplier-grid" role="status">Carregando fornecedores…</div>';const hero=document.querySelector('.hero');if(hero)hero.after(section);else document.body.append(section);async function load(){const root=document.getElementById('partnerDirectory'),j=await A.api('portal_fornecedores_publicos');if(!j.sucesso){root.innerHTML='<p>Não foi possível carregar este conteúdo. <button class="arq-btn" id="retryDirectory">Tentar novamente</button></p>';document.getElementById('retryDirectory').onclick=load;return;}root.innerHTML=(j.fornecedores||[]).map(s=>`<a class="arq-supplier-card" href="fornecedor.html?id=${encodeURIComponent(s.id)}"><img ${M.attributes(M.getSupplierImage(s),s.categoria)} alt="Identidade de ${A.esc(s.nome)}" width="96" height="96" loading="lazy"><div><b>${A.esc(s.nome)}</b><p>${A.esc(s.cidade||s.categoria)}</p><span>Cadastro verificado</span></div></a>`).join('')||'<p>Ainda não há fornecedores publicados.</p>';M.hydrate(root);}load();})();
